# CLAUDE.md

This file provides guidance for AI assistants working with this repository.

## Repository

- **Name**: b
- **Owner**: marcusbooker77
- **Status**: New/empty repository — project scaffolding has not yet been added.

## Getting Started

This repository does not yet contain source code or configuration. When setting up the project:

1. Initialize the project with appropriate tooling (e.g., `npm init`, `cargo init`, etc.)
2. Add a `.gitignore` appropriate for the chosen language/framework
3. Set up linting and formatting from the start
4. Add a README.md describing the project's purpose

## Conventions

### Git Workflow

- Use descriptive branch names prefixed by category (e.g., `feature/`, `fix/`, `docs/`)
- Write clear, concise commit messages in imperative mood (e.g., "Add user auth endpoint")
- Keep commits focused — one logical change per commit

### Code Quality

- Run linters and formatters before committing
- Write tests alongside new features
- Avoid committing secrets, credentials, or `.env` files

## Notes for AI Assistants

- Read existing code before proposing changes
- Prefer editing existing files over creating new ones
- Keep changes minimal and focused on the task at hand
- Do not introduce unnecessary dependencies
- Update this CLAUDE.md as the project evolves with specific build commands, test instructions, and architectural decisions
